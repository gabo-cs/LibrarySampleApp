<div>
  <h1>Libros</h1>

  <table>
    <thead>
      <tr>
        <th>Id</th>
        <th>Título</th>
        <th>ISBN</th>
        <th>Author</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @books.each do |book| %>
        <tr>
          <td><%= book.id %></td>
          <td><%= book.title %></td>
          <td><%= book.isbn %></td>
          <td><%= link_to book.author.name, author_path(book.author) %></td>
          <td><%= link_to "Show", book %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br />

  <%= link_to "Nuevo Libro", new_book_path %><br />
  <%= link_to "Ver Autores", authors_path %>
</div>
